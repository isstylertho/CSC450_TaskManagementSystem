# this is the file for backend. lets build this as a flask application and link with flasks SQLite DB
# to keep everything local handled by SQLAlchemy

# this will handle the main logic for all functions serving as a backend API we can just call to 
# perform needed functionality
